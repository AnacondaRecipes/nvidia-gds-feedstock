build_parameters:
  - "--suppress-variables"
  #- "--skip-existing"
  - "--error-overlinking"

staging_channel_upload_target: ctk-13.0.2-build-4

channels:
  - https://staging.continuum.io/pbp/ctk-13.0.2-build-4
